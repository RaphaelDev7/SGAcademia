package dao;


public class LoginDao {
    
    public void realizarLogin(String cpf, String senha) {
        /*
            ConnectionFactory conectBD = new ConnectionFactory();

            conectBD.getConexao();
            stm = conectBD.getConexao().createStatement();

            String sql = "SELECT * FROM sgacademia.funcionarios";
            conectBD.rs = stm.executeQuery(sql);
            conectBD.rs.first();

            if (jTextCpf.getText().equals(conectBD.rs.getString("cpf_func"))
                    && jTextSenha.getText().equals(conectBD.rs.getString("senha_func"))) {
                JOptionPane.showMessageDialog(this, "Acesso Permitido !");
                    new Principal().setVisible(true);
                    dispose();
            } else {
                JOptionPane.showMessageDialog(this, "Acesso Negado !");
                return;
            }

        } catch (SQLException e) {
                JOptionPane.showMessageDialog(this, "Acesso Negado !");
        }
        */
    }
    
}
